name: Deploy Backend

on:
  push:
    branches: [main]

jobs:
  deploy:
    name: Deploy to Railway
    runs-on: ubuntu-latest

    steps:
      - name: Deploy
        run: echo "Deploy to Railway — real config in Week 2"
